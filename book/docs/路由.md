# 路由

执行下面的代码，处理不同类型的 HTTP 请求。

```javascript
const express = require('express');
const app = express();
app.get('/', (req, res) => {
    res.send('GET');
});
app.post('/', (req, res) => {
    res.send('POST');
});
app.listen(9000, () => console.log('Server is running.'));
```

